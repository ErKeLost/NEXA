{
  "name": "@nexa/mastra",
  "version": "0.1.0",
  "private": true,
  "description": "Mastra integration layer for NEXA: env validation, storage, runtime context, toolsets, and shared instance.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist node_modules",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.64",
    "@mastra/core": "^0.24.8",
    "@mastra/memory": "^0.15.13",
    "@mastra/pg": "^0.17.10",
    "@nexa/agents": "workspace:*",
    "@nexa/shared": "workspace:*",
    "@nexa/tools": "workspace:*",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
